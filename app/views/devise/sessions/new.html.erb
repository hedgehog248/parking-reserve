<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

<div class="main">
  <h2 class="page-title"><%= t('.sign_in') %></h2>
  <div class="message">
    Eメールとパスワードを入力してログインボタンを押してください。<br>
    初めての方は「アカウント登録」を押して登録画面に移動してください。
  </div>

  <div class="form-wrapper">
    <div class="alert">
      <%= alert %>
    </div>
    <div class="form">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :email, class: "required" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "text-field" %>
        </div>

        <div class="field">
          <%= f.label :password, class: "required" %><br />
          <%= f.password_field :password, autocomplete: "current-password", class: "text-field" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit t('.sign_in'), class: "colored-btn btn" %>
        </div>
      <% end %>
    </div>
    <div class="links">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>