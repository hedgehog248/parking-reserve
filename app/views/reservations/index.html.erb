<div class="main">
  <h1>予約表</h1>
  翌月末まで予約することができます。
  <div class="">
    <%= month_calendar do |date| %>
      <%#= date.day %>
      <%= link_to "#{date.day}", reservation_path(date) %>
    <% end %>
  </div>
</div>